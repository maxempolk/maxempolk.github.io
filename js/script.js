// Здесь вы можете добавить JavaScript для интерактивных элементов,
// например, для плавной прокрутки к секциям при клике на ссылки в навигации.

// Пример плавной прокрутки (раскомментируйте, если нужно):
/*
document.querySelectorAll('nav a').forEach(anchor => {
    anchor.addEventListener('click', function (e) {
        e.preventDefault();

        document.querySelector(this.getAttribute('href')).scrollIntoView({
            behavior: 'smooth'
        });
    });
});
*/